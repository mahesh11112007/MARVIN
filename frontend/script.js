function handleAction(action) {
    console.log(`Action clicked: ${action}`);
    alert(`You clicked: ${action}\n\nThis is a demo. Connect to backend endpoints via POST requests:\n- /create\n- /upload\n- /analyze\n- /optimize`);
}
